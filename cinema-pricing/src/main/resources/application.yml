spring:
  application:
    name: cinema-pricing

server:
  port: 8083

grpc:
  server:
    port: 9090

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics
  endpoint:
    health:
      show-details: always
